# -*- mode: ruby -*-
# vi: set ft=ruby :

#VAGRANT_HOME            = "~/"
#VAGRANT_DOTFILE_PATH    = "~/.vagrant.d/"
#VAGRANT_LOG             = "~/.vagrant.d/logs/vagrant.log"
#VAGRANT_DEBUG_LAUNCHER  = "~/.vagrant.d/logs/debug_launcher.log"
VAGRANTFILE_API_VERSION = "2"
#VAGRANT_INFO=debug

Vagrant.configure(VAGRANTFILE_API_VERSION, &Oscar.run(File.expand_path('../config', __FILE__))) if defined? Oscar
